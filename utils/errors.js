// client-side errors
const BAD_REQUEST = 400;
const NOT_FOUND = 404;
const CONFLICT = 409;

// server errors
const INTERNAL_SERVER_ERROR = 500;

module.exports = {
  BAD_REQUEST,
  NOT_FOUND,
  CONFLICT,
  INTERNAL_SERVER_ERROR,
};
